class World

instance variables

public static done: bool := false

operations

public run : () ==> ()
run() == (
  start(System`controller);
  start(System`env);
  block();
 );

private block : () ==>()
block() == skip;

sync per block => done;

end World